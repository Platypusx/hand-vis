FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn
COPY server.py .
# data/ is mounted at runtime via compose volume
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]
